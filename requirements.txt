mcp==2.2.0
# 2.2.6, not 2.5.x: numpy >=2.5 requires Python >=3.12, and the CI matrix
# runs 3.11-3.13. 2.2.6 officially supports 3.10-3.13 (ST needs >=1.24).
numpy==2.2.6
sentence-transformers==6.0.1
pytest==9.1.1

# Notes:
# - Tested on Python 3.11-3.13 (CI matrix); developed on 3.13 (Windows).
# - CI also lint-checks (ruff, see ruff.toml) and builds the lean Docker
#   image, which installs only mcp + numpy directly.
# - sentence-transformers is optional at runtime (MCP_EMBEDDINGS=off runs
#   the server without it) but required for the Phase 4 semantic tests.
# - The all-MiniLM-L6-v2 model (~80MB) downloads to the local HF cache on
#   first use; after that the server forces offline mode automatically.
